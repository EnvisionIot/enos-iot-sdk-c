AUX_SOURCE_DIRECTORY(. DIR_APP_SRCS)
STRING(REGEX REPLACE ".*/(.*)" "\\1" CURRENT_DIR_NAME ${CMAKE_CURRENT_SOURCE_DIR})
SET(CURRENT_APP_NAME "${CURRENT_DIR_NAME}")
ADD_EXECUTABLE(${CURRENT_APP_NAME} ${DIR_APP_SRCS})
TARGET_LINK_LIBRARIES(${CURRENT_APP_NAME} enos_c_api)